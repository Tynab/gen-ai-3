# Môi trường ảo & biến môi trường (chứa API key — tuyệt đối không commit).
# .env.example thì NGƯỢC LẠI: có commit, vì nó là bản mẫu rỗng key.
.venv/
.env

# Python
__pycache__/
*.py[cod]

# Jupyter
.ipynb_checkpoints/

# Cache của pytest (repo không dùng ruff/mypy nên không liệt kê cache của chúng)
.pytest_cache/

# IDE
.vscode/
.idea/

# Hệ điều hành
Thumbs.db
.DS_Store
